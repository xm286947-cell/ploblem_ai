# AI PATCH03 DELIVERY

Version: KNOWLEDGE_QUALITY_ISSUE_ANALYSIS_ENGINE_V1.0_ADDENDUM01_A7_RC1_AI_PATCH03
Status: AI Runtime Stability + Smart Re-analysis

## Fixed
1. Stage-specific runtime
   - occurrence: 120s / 1 retry
   - escape: 120s / 1 retry
   - recurrence: 240s / 2 retries
   - capability_gap: 300s / 2 retries
   - validation retry remains separately configurable

2. Capability Gap persistence
   - gap_id is always generated by Engine UUID
   - LLM-provided gap_id is ignored
   - prevents UNIQUE constraint collision

3. Context slimming
   - recurrence receives compact issue facts + occurrence/escape + existing controls
   - capability_gap receives compact issue facts + structured occurrence/escape/recurrence
   - raw/debug payloads are not re-injected into later stages

4. Stale RUNNING recovery
   - RUNNING older than stage timeout + 60s buffer becomes FAILED
   - error: STALE_RUNNING_TIMEOUT
   - retry remains available

5. Smart analysis reuse
   - Current Issue Version completed stages are skipped by default
   - failed/missing stages continue
   - new Issue Version automatically re-analyzes because old result is not current
   - Web/CLI/API expose force re-analysis
   - Issue Detail provides "分析未完成项" and "强制重新分析"

## Compatibility
- Existing model.yaml top-level timeout/max_retries still work as fallback.
- Existing data and Analysis Runs are preserved.
- Original / Normalized / Human Analysis / Mapping are unchanged.

## Test
Focused PATCH03 tests: 4 passed
Full regression: 193 passed / 0 failed
