agent_id: major_issue.repeat_case
version: v1
label: Major Issue Repeat Case Decision
model_ref: qwen_prod

prompt:
  ref: prompts/repeat_decision.md
  version: M8.4-P1

output_schema:
  ref: RepeatDecisionDTO
  version: "1.0.0"

execution:
  timeout_seconds: 180
  retry:
    transport_attempts: 2
    validation_attempts: 2
    step_attempts: 1
  budget:
    max_provider_calls_per_step: 2
  model:
    temperature: 0.1
    max_tokens: 2400

metadata:
  business_domain: MAJOR_CASE
  analysis_stage: repeat_decision
  provider_response_shape: json_object
  what_how_boundary: RUNTIME_EXECUTION_ONLY
