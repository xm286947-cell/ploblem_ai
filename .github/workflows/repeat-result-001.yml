name: REPEAT-RESULT-001 Gate

on:
  push:
    branches:
      - "feature/repeat-result-001"
  pull_request:
    branches:
      - main
    paths:
      - "quality_knowledge/repeat_risk/**"
      - "docs/contracts/REPEAT_RESULT_001.md"
      - "tests/test_repeat_result_contract.py"
      - ".github/workflows/repeat-result-001.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repeat-result:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Compile
        run: python -m compileall -q quality_knowledge/repeat_risk

      - name: REPEAT-RESULT-001 contract tests
        run: python -m pytest -q tests/test_repeat_result_contract.py

      - name: REPEAT-SEARCH regression
        run: python -m pytest -q tests/test_repeat_search_contract.py

      - name: REPEAT-ITR regression
        run: python -m pytest -q tests/test_repeat_itr_subject.py

      - name: Historical Case and Case Publish regression
        run: python -m pytest -q tests/test_historical_case_consumer_contract.py tests/test_case_publish_adapter.py tests/test_case_publish_service.py
