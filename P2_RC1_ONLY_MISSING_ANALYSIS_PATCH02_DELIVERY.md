# P2 RC1 仅分析未完成项 PATCH02

## 修复

- “仅分析未完成项”在创建任务前按当前问题版本检查四个阶段。
- `occurrence`、`escape`、`recurrence`、`capability_gap` 均已完成的问题直接排除。
- 被排除的问题不进入队列、不调用模型、不计入本次任务完成数。
- 服务层同时保留二次过滤，防止 API 或其他入口绕过页面筛选。
- 批量结果增加 `requested_total` 和 `skipped_completed`，可诊断请求数与跳过数。

## 兼容性

- 数据库 Schema 无变化。
- 原有“强制重新分析已完成项”行为保持不变。
- 存在任一未完成阶段的问题仍会进入任务，但已完成阶段继续复用。

## 验证

- 专项回归：14 passed
- 正式测试目录全量回归：382 passed
