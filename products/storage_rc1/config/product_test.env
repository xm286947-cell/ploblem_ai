# Storage product-test defaults. No secret is stored here.
# Runtime is a public platform dependency; Storage owns only upper-layer adaptation.
STORAGE_WEB_HOST=0.0.0.0
STORAGE_WEB_PORT=8765
RUNTIME_PROVIDER_TRACE=1
STORAGE_RUNTIME_HTTP_TRACE=1
STORAGE_APP_LOG_STDOUT=1
