# P2 RC1 多智能体累计修复 PATCH06

这是 PATCH03、PATCH04、PATCH05 的累计包，解决只安装后续增量包时运行时仍写 `DEFAULT` 的问题。

## 包含能力

- 多智能体配置加载。
- 四阶段动态路由。
- 单问题与批量分析智能体选择。
- 实际智能体写入运行审计记录。
- 历史记录显示智能体与模型。
- 同模型诊断安全兼容。
- `GET /api/analysis-agents` 返回当前四阶段实际解析结果。

## 部署

1. 覆盖本包同路径文件。
2. 不覆盖现有 `config/model.yaml`；保留你已配置的不同模型。
3. 重启服务。
4. 打开 `/api/analysis-agents`，确认每个阶段的 `agent_id` 和 `model`。
5. 对已完成问题验证新模型时，必须选择“强制重新分析”；旧历史记录仍会显示 `DEFAULT`，新运行记录才会记录实际智能体。

## 验证

- 专项：18 passed
- 全量：386 passed
- 数据库 Schema：无变化
