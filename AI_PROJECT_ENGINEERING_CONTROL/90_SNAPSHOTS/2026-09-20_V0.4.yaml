snapshot_version: "0.4"
snapshot_date: "2026-09-20"
repository: "xm286947-cell/ploblem_ai"

control_branch: "feature/engineering-control-v0.1"

frozen_design:
  ai_runtime_contract: "20_SHARED_CONFIG/AI_RUNTIME_CONTRACT.yaml"
  execution_engine_contract: "20_SHARED_CONFIG/EXECUTION_ENGINE_CONTRACT.yaml"
  packaging_policy: "20_SHARED_CONFIG/PACKAGING_POLICY.yaml"

migration_strategy: ADAPTER_FIRST
migration_order:
  - EXECUTION_ENGINE
  - AI_RUNTIME
  - PACKAGING_POLICY
  - DEPRECATION

project_baselines:
  quality_scenario: "c7aa3017e07f46d1b010c90300becdae9ccd7005"
  major_problem: "18ac9ea4c8919be17ef82980766bcaba30e719cb"

design_decisions:
  - "业务项目保留领域策略，公共层收敛执行机制。"
  - "新 ThreadPool / Provider Client / Packaging Policy 实现进入禁止新增目标。"
  - "Mock 与真实模型结果必须显式区分。"
  - "Packaging 的运行数据、sources/、数据库与敏感信息统一禁止打包。"

next_target:
  version: "0.5"
  focus:
    - "Execution Engine adapter 最小实现"
    - "迁移 builder/parallel_execution.py"
    - "选择一个低风险 consumer 做首个接入验证"
