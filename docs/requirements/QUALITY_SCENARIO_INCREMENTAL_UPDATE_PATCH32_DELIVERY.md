# PATCH32：质量场景增量增强与数据组边界

日期：2026-09-06。目标基线：`v1.1-p2-rc2-full-20260901`，累计包含 PATCH31。

## 完成内容

- ITR-only 可以先生成候选；同号彻底解决单后续导入并再次人工生成时，增强原候选而不是创建第二个质量场景。
- 原场景编号不变，版本号递增，同时保留 ITR 与 CS 两份来源证据；任务明细显示“已增强原候选”。
- 已发布或质量分类已人工确认的场景禁止自动覆盖，新增证据进入待人工评审并链接原场景。
- 任务覆盖账本增加“增强”数量，与新识别、复用、待确认、失败分别统计。
- 场景生成页增加允许数据组多选。关联只读取选中组；同一类型出现多个有效来源时标记冲突，不任意选择。
- 直接调用生成服务时，会按材料真实类型分派到 CS/ITR 或软件考核来源，不再把所有材料编号误当软件考核数据。

## 验证结果

- 全量自动回归：472 passed。
- 新增用例覆盖 ITR-only → CS 到达 → 原位增强，以及跨组冲突与指定组隔离。
- 未使用真实内部数据。真实模型批量语义质量、Win10 BAT 双击启动仍需在目标环境验收。

## 升级方式

停止 BAT 服务并备份程序目录、SQLite 数据库；将 PATCH32 累计包按原目录结构覆盖，继续使用原 `start_quality_capability_p1.bat`。若从本轮完整包开始，可直接使用 20260906 R2 完整包。
