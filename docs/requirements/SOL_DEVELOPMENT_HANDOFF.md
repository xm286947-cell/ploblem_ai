# Sol 后续开发交接与执行台账

日期：2026-09-05。执行模型指定：**GPT-5.6 Sol**。本文件是交接约定，不代表当前会话已切换模型或开发已启动。

## 必读顺序

1. 仓库根目录 `AGENTS.md`。
2. [REQ-020 方案与规则基线](REQ-020_CUSTOMER_QUALITY_SCENARIO_PORTRAIT_BASELINE_V1.md)。
3. [需求总目录](REQUIREMENT_INDEX.md)。
4. 现有 PATCH30/场景验收说明及与当前阶段相关的代码。

## 当前起点

现有代码起点为 `aa0f61d`（PATCH30），已有场景语义、词典、来源、任务、资产和看板能力。不要重建系统，不把本轮计划写成已完成。

工作目录：`p0_integration/KNOWLEDGE_QUALITY_ISSUE_ANALYSIS_ENGINE_V1.1_QUALITY_CAPABILITY_P0_RC1`，不是 `sources/`。

本轮归档未改应用代码，未生成新功能包。现有 raw_evidence、raw_excel、output 下的未提交改动不是本任务资产；不得清理、提交或纳入发布包。

## 执行方式

后续开发由 Sol 承担。开发模型指定不改变应用内 DEFAULT/多 Agent/model.yaml 配置，也不把用户本地代理写入产品配置。若承接会话不是 Sol，应明确提示用户选择 Sol，不声称已切换。

从 M0 只读核对开始，记录实际代码位置与验证结果，然后按 M1–M5 最小改动实施。每阶段完成更新下表及证据链接。范围内实现不反复等待确认；扩范围、外部发布、内部真实数据使用另行确认。

| 阶段 | 初始状态 | 完成证据 |
|---|---|---|
| M0 框架/字段/兼容核对 | TODO | 待补代码定位与缺口清单 |
| M1 多源接入、隔离与跨批去重 | TODO | 待补自动测试及逐项台账 |
| M2 场景解释与证据增强 | TODO | 待补阶段反例、硬件和来源回归 |
| M3 客户/行业跨产品画像 | TODO | 待补浏览器业务流验收 |
| M4 洞察与样例价值验证 | TODO | 待补发现→证据→下一步示例 |
| M5 打包、升级与交付 | TODO | 待补包清单/校验/兼容记录 |

## 开始执行提示词

按仓库 AGENTS.md 和 REQ-020 基线，由 GPT-5.6 Sol 完成后续开发。从 M0 核对既有实现，再顺序实施 M1–M5。先修受控多源取数、标准 ITR 跨批去重和字段证据，再做客户/行业画像。不得重建系统或开发治理闭环，不接触真实内部数据，保留现有 BAT 入口。以合成 SQLite 数据完成业务流及回归验收，逐项记录未通过和未验证，不把页面出现当成功能闭环。

## 验收和交付要求

- 测试先检查运行环境。历史可用命令：`PYTHONPATH=tests ../../.test-venv/bin/python -m pytest -q tests --import-mode=importlib`；路径不存在则使用项目已声明依赖建立隔离环境，不能假定历史结果仍有效。
- 新增关键合成用例：同号 CS/ITR 不重复、并发重复提交、ITR 后补 CS、分组冲突、59 条覆盖、硬件条件不串用、产品词典、年份/月口径、人工内容保存。
- 浏览器验收：选择来源/范围 → 预览 → 生成 → 状态 → 候选 → 审核 → 画像 → 下钻。检查空值、失败、重试、重启，不只单测函数。
- 单独说明真实模型语义质量和 Win10 BAT 是否实际验证；未验证明确列出。
- 每次提交只包含代码、文档、合成测试；不得提交原始内部材料、运行数据库、密钥、日志和用户既有改动。
- 发布前核对基线与打包脚本。升级包必须覆盖相对目标基线的完整依赖、模板和迁移，不能仅打本次改动造成连续漏补。完整包不夹带真实数据。
- 最终交付：完成项、未完成项、测试结果、升级/完整包及校验值、BAT 使用说明、备份/回滚步骤。不未测先宣称流程跑通。
