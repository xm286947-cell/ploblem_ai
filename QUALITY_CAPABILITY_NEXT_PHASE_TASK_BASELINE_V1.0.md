# QUALITY CAPABILITY NEXT PHASE TASK BASELINE V1.0

状态：`LOCKED`

基线：稳定老版前端、老版业务流程、老数据库为唯一主系统；`QUALITY_CAPABILITY_LEGACY_INTEGRATION_RC1` 为新增能力起点。

## 一、不可变原则

1. 不重建问题工作台、问题详情、人工分析、Mapping、数据接入和导出页面。
2. 不修改老业务表结构；新增能力使用 `qc_*` 扩展表，通过 `knowledge_id`、`issue_version_id`、`analysis_run_id` 关联。
3. AI 结论必须同时保存来源、证据、置信度和待确认状态；信息不足时必须明确标识，不得编造。
4. 人工确认值优先于 AI 值，但必须保留 AI 原值、人工修订值和审计记录。
5. 洞察必须能下钻到真实问题，并显示统计口径、覆盖率和来源比例。

## 二、P2 锁定范围

### P2-A AI 原生质量分析契约

四阶段分析直接输出：

- 发生 MRC、流出 MRC。
- 质量工程能力标签：需求、设计、实现、集成、测试、发布、软硬协同等。
- 质量管理能力标签：流程、变更、配置、分支合入、发布门禁、已知问题、闭环和横向复制等。
- 生命周期阶段、问题领域、多标签问题类型。
- 客户影响、再发风险、建议措施和验证指标。
- 来源、证据、置信度和需要人工确认的问题。

### P2-B 人工确认与有效结论

- 原问题详情继续承载人工分析和 AI 待确认。
- 展示 AI 原值、人工修订值和当前有效值。
- 人工确认 MRC、能力标签、生命周期和硬件结论后，洞察立即更新。
- 保存修改人、修改时间、修改原因、证据及影响范围。

### P2-C 质量洞察闭环

- 每个业务分别展示质量工程关键矛盾 TOP3、质量管理关键矛盾 TOP3。
- 展示 MRC×质量能力、生命周期×质量能力矩阵。
- 展示原始根因 L1-L4、测试 L1-L4 与 AI 分类的一致、冲突和补充关系。
- 展示总问题数、已分析数、分类覆盖率、来源比例和人工确认率。
- 支持产品、月份、领域、生命周期联合筛选和精确问题下钻。
- 所有编码提供中文名称。

### P2-D 软件问题中的硬件关联与器件失效分析（新增诉求）

当前适用范围：分析对象仍以软件问题为主。当软件问题被识别或人工确认为与硬件、器件、板卡、接口或软硬协同因素相关时，补充记录硬件信息和失效原因。

当前不要求建立完整的纯硬件质量分析体系，但数据结构不得限制未来扩展。将来可以在同一结构上支持硬件问题本身，以及机械、电气、材料、制造等更完整的失效分析。

一个问题允许关联多个器件，每个器件记录：

- 与当前软件问题的硬件相关性：`NOT_RELATED / POSSIBLE / CONFIRMED`。
- 器件类别、器件名称、厂家、型号/料号。
- 批次/Lot、序列号、供应商。
- 所在板卡/模块、位号/安装位置、硬件版本。
- 失效模式：器件表现出的失效现象。
- 失效机理：电气、热、机械、材料、环境、制造、老化等机理。
- 失效原因：设计选型、降额、来料、制造、装配、使用环境、维护、软硬接口、驱动适配、时序/资源约束或未知。
- 对整机/客户的影响、复现条件、检测方法和处置措施。
- 来源、证据、置信度、是否经过人工确认。

实现约束：

- 新增 `qc_issue_hardware_component` 和 `qc_hardware_failure_analysis` 扩展结构，不改老问题表。
- 原始 Excel 已有器件字段时优先使用源数据；缺失时允许 AI 提取，但必须标记为 AI 推断。
- 证据不足时产生人工确认事项，不允许直接判定具体厂家、料号或失效机理。
- 原问题详情新增“硬件关联与器件失效”区域，人工分析可补充和修订。
- 质量洞察支持按器件类别、厂家/型号、失效模式、失效机理和失效原因聚合，并可下钻问题。
- 当前洞察回答“哪些软件问题受到硬件因素影响、主要涉及什么器件和失效原因”；未来再扩展为独立的硬件质量洞察。

### P2-E 批量分析与真实数据验收

- 批量任务持久化，服务重启后仍可查看。
- 展示每个问题当前阶段、耗时、错误原因，并支持只重试失败项。
- 使用 PLC、HMI、IFA、机器人以及硬件相关真实样本验收。
- 由质量人员确认关键矛盾是否具有业务价值，并据此修订 Prompt/词典。
- 老业务流程回归必须全部通过。

## 三、P2 明确不做

- 不迁移或替换老数据库。
- 不重写老前端。
- 不引入 MySQL、Docker 或飞书数据库改造。
- 不在本阶段重做正向风险评估工作台。
- 不做复杂的产品多领域角色编排；仍以“问题属性”判断软件、硬件或机械相关性。

## 四、后续 P3

- 正向风险案例库与需求/设计/测试材料评估正式接入老系统。
- Prompt、分类词典、MRC、生命周期、能力词典和模型参数的版本化配置页面。
- 评估报告版本比较、案例发布和权限治理。

## 五、P2 完成判定

只有同时满足以下条件才可宣布完成：

1. 老版全部业务活动仍可使用。
2. AI 原生输出新契约，不再只依赖旧字段投影。
3. 人工修订能改变当前有效结论和洞察结果。
4. 软件问题中涉及硬件因素时，可以记录多个器件及失效原因并参与洞察，同时结构支持未来纯硬件问题扩展。
5. 关键矛盾、矩阵、覆盖率和下钻使用真实数据闭环。
6. 专项测试和完整工程回归均为 0 failed。
